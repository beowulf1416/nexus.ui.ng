import { BASE_URL } from "core";

export const URLS = {
  organization_save: `${BASE_URL}/organizations/save`,
  organizations_fetch: `${BASE_URL}/organizations/fetch`,
  organizations_fetch_tree: `${BASE_URL}/organizations/fetch/tree`
};
